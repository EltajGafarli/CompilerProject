spring:
  application:
    name: SDPProject
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 190407011
    url: jdbc:mysql://localhost:3306/sdpproject

  jpa:
    generate-ddl: true
    show-sql: true
    database: mysql
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect
    hibernate:
      ddl-auto: update
    open-in-view: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: todolistorganization@gmail.com
    password: rgankytzymfbyphy
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  rabbitmq:
    listener:
      simple:
        auto-startup: false
  liquibase:
    enabled: false
    change-log: db/changelog-master.yaml

server:
  port: 8088

admin:
  email: "eltajgafarli@gmail.com"
